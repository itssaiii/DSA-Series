#include<bits/stdc++.h>
using namespace std;
void Process(string str, string term,string rep);
int main(){
    string str,rep;
    cout<<"Enter a string"<<endl;
    getline(cin,str);
    string term;
    cout<<"Enter a string"<<endl;
    getline(cin,term);
    cout<<"Enter a string"<<endl;
    getline(cin,rep);
    Process(str,term,rep);
    return 0;
}
void Process(string str,string term,string rep){
    
}