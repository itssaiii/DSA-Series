#include<bits/stdc++.h>
using namespace std;
int Process(string str);
int main(){
    string str;
    cout<<"enter a string"<<endl;
    getline(cin,str);
    int ans = Process(str);
    return 0;
}
int Process(string str){
    int j=0;
    vector<int> vec;
    for(int i=0;i<str.length();i++){
        if(isdigit(str[i])){
            char ch = str[i];
             
        }
    }
}